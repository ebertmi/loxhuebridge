import { Routes } from '@angular/router';
import { ScenesPageComponent } from './components/scenes-page/scenes-page.component';

export const SCENES_ROUTES: Routes = [
  { path: '', component: ScenesPageComponent }
];

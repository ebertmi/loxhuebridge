import { Routes } from '@angular/router';
import { DiagnosticsPageComponent } from './components/diagnostics-page/diagnostics-page.component';

export const DIAGNOSTICS_ROUTES: Routes = [
  { path: '', component: DiagnosticsPageComponent }
];
